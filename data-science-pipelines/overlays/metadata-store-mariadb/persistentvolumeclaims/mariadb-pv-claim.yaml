apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    application-crd-id: data-science-pipelines
  name: mariadb-pv-claim
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
